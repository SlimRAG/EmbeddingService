# Draft

- `.md` => bge-m3
- `.pdf`, `.doc`, `.docx` => MinerU => bge-m3
- `.html` => markitdown => bge-m3
- `.webp` => `.jpg` => CLIP
- `.jpg`, `.png` => CLIP
- `.arw` => `.jpg` + demosaic => CLIP
- `.mp4` => Extract keyframes => CLIP
